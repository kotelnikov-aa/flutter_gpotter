import 'package:flutter/material.dart';

class SpelsScreen extends StatelessWidget {
  const SpelsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Spels SCREEN'));
  }
}
