import 'package:flutter/material.dart';

class WizartsScreen extends StatelessWidget {
  const WizartsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Wizarts SCREEN'));
  }
}
